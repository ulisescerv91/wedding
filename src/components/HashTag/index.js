import React from "react";
import styled from "styled-components";

export function HashTag() {
  return (
    <HashTagContainer>
      <h2>DURANTE NUESTRA BODA UTILIZA EL HASHTAG</h2>
      <div>#bodorrioC+D</div>
    </HashTagContainer>
  );
}

const HashTagContainer = styled.div``;
